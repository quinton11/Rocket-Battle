#include "Enemy.h"

void Enemy::update(double dt)
{
    // takes in deltatime and updates
    // rocket position
}

void Enemy::render(SDL_Renderer *renderer, SDL_Texture *texture, double dt) 
{
    //render function for enemy ships
}